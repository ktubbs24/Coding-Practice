// Currency Converter 

// I want to convert AUD (Australian Dollars) to USD (United States Dollars) using the $AUD to $USD Formula: 

//   AUD = USD * 1.5


let USD = 1200; 
let AUD = 1200 * 1.5;  

console.log("$" + AUD); 
// The answer is $1800 AUD

/* or it can be written this way to come up with the same answer (I just prefer the first way because it is the same data types on both sides of the equation):

let USD = 1200; 
let AUD = USD * 1.5;  

console.log("$" + AUD); 


*/
